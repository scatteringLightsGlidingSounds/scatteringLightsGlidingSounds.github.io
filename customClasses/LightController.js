function LightController{
    var numberOfRepFlowerPots, numberOfPreviewFlowerPots, numberOfPoles;

    function setup(flowerMinAlpha, flowerMaxAlpha, poleMinAlpha, poleMaxAlpha){
        // load flowerPot json file

        var 
    }
}